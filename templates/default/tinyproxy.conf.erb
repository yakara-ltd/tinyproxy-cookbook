##
## Managed by Chef
##

<%

config=''

node[:tinyproxy][:conf].sort.each do |setting, value| 
  if !value.nil? and value != ''
    if value.is_a? Array
      value.each do |v| 
        config << "#{setting} #{v}\n"
      end 
    else 
      config << "#{setting} #{value}\n"
    end 
  end 
end

-%> 
<%= config -%>